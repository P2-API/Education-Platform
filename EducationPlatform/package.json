{
  "name": "educationplatform",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "node ./Backend/server.cjs",
    "both": "concurrently \"npm run serve\" \"npm run dev\"",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "concurrently": "^8.2.2",
    "vite": "^5.1.0"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "express": "^4.18.2"
  }
}
